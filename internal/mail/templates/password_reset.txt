LiveTranslate 密码重置 / Password reset

你（或他人）请求重置 LiveTranslate 账号的密码。请在 {{.ResetLinkMinutes}} 分钟内通过下面的链接完成重置：
Someone requested a password reset for your LiveTranslate account. Use the following within {{.ResetLinkMinutes}} minutes:
{{if .Link}}
{{.Link}}

如果没有 App，链接会打开一个网页，其中也提供了在 App 中继续操作的方式。
If the app is not installed, the link opens a web page with a way to continue in the app.
{{else}}
重置凭证（复制后在 App 的「找回密码」页粘贴）:
Reset token (paste it in the app's password recovery page):

{{.Code}}
{{end}}
如果你没有请求重置密码，请忽略本邮件，你的密码不会改变。
If you did not request this, ignore this email — your password is unchanged.

— LiveTranslate
